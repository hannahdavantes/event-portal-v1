import customFetch from "../../utils/customFetch";

export const loader = async () => {
  //   try {
  //     const data = customFetch.get("/users/me");
  //   } catch (error) {}

  return "JELLO";
};
